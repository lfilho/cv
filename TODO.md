# TODO

## Agent readiness follow-up

- Push to `main`, wait for the GitHub Actions deploy, then re-run the curl checks from AGENT_READINESS_LUIZ_DEV_PLAN.md against `https://luiz.dev`.

## Opengraph image

- Generate a real `og:image` at `/images/og.png` (currently a placeholder referenced in metadata).
